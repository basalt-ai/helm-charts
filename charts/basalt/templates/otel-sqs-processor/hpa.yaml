{{- if .Values.otelSqsProcessor.autoscaling.hpa.enabled }}
apiVersion: {{ include "common.capabilities.hpa.apiVersion" ( dict "context" $ ) }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "basalt.otel-sqs-processor.fullname" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: otel-sqs-processor
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: {{ include "common.capabilities.deployment.apiVersion" . }}
    kind: Deployment
    name: {{ include "basalt.otel-sqs-processor.fullname" . }}
  minReplicas: {{ .Values.otelSqsProcessor.autoscaling.hpa.minReplicas }}
  maxReplicas: {{ .Values.otelSqsProcessor.autoscaling.hpa.maxReplicas }}
  metrics:
    {{- if .Values.otelSqsProcessor.autoscaling.hpa.targetMemory }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.otelSqsProcessor.autoscaling.hpa.targetMemory }}
    {{- end }}
    {{- if .Values.otelSqsProcessor.autoscaling.hpa.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.otelSqsProcessor.autoscaling.hpa.targetCPU }}
    {{- end }}
{{- end }}
