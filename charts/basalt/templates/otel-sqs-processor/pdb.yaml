{{- if .Values.otelSqsProcessor.pdb.create }}
apiVersion: {{ include "common.capabilities.policy.apiVersion" . }}
kind: PodDisruptionBudget
metadata:
  name: {{ include "basalt.otel-sqs-processor.fullname" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: otel-sqs-processor
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.otelSqsProcessor.pdb.minAvailable }}
  minAvailable: {{ .Values.otelSqsProcessor.pdb.minAvailable }}
  {{- end  }}
  {{- if or .Values.otelSqsProcessor.pdb.maxUnavailable ( not .Values.otelSqsProcessor.pdb.minAvailable ) }}
  maxUnavailable: {{ .Values.otelSqsProcessor.pdb.maxUnavailable | default 1 }}
  {{- end  }}
  {{- $podLabels := include "common.tplvalues.merge" ( dict "values" ( list .Values.otelSqsProcessor.podLabels .Values.commonLabels ) "context" . ) }}
  selector:
    matchLabels: {{- include "common.labels.matchLabels" ( dict "customLabels" $podLabels "context" $ ) | nindent 6 }}
      app.kubernetes.io/component: otel-sqs-processor
{{- end }}
