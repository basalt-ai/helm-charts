{{- if .Values.jobsRunner.autoscaling.hpa.enabled }}
apiVersion: {{ include "common.capabilities.hpa.apiVersion" ( dict "context" $ ) }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "basalt.jobs-runner.fullname" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: jobs-runner
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: {{ include "common.capabilities.deployment.apiVersion" . }}
    kind: Deployment
    name: {{ include "basalt.jobs-runner.fullname" . }}
  minReplicas: {{ .Values.jobsRunner.autoscaling.hpa.minReplicas }}
  maxReplicas: {{ .Values.jobsRunner.autoscaling.hpa.maxReplicas }}
  metrics:
    {{- if .Values.jobsRunner.autoscaling.hpa.targetMemory }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.jobsRunner.autoscaling.hpa.targetMemory }}
    {{- end }}
    {{- if .Values.jobsRunner.autoscaling.hpa.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.jobsRunner.autoscaling.hpa.targetCPU }}
    {{- end }}
{{- end }}
