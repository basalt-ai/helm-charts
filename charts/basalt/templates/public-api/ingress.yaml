{{- if .Values.publicApi.ingress.enabled }}
apiVersion: {{ include "common.capabilities.ingress.apiVersion" . }}
kind: Ingress
metadata:
  name: {{ template "basalt.public-api.fullname" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: public-api
  {{- if or .Values.publicApi.ingress.annotations .Values.commonAnnotations }}
  {{- $annotations := include "common.tplvalues.merge" (dict "values" (list .Values.publicApi.ingress.annotations .Values.commonAnnotations) "context" .) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.publicApi.ingress.ingressClassName }}
  ingressClassName: {{ .Values.publicApi.ingress.ingressClassName | quote }}
  {{- end }}
  rules:
    {{- if .Values.publicApi.ingress.hostname }}
    - host: {{ .Values.publicApi.ingress.hostname }}
      http:
        paths:
          {{- if .Values.publicApi.ingress.extraPaths }}
          {{- toYaml .Values.publicApi.ingress.extraPaths | nindent 10 }}
          {{- end }}
          - path: {{ .Values.publicApi.ingress.path }}
            pathType: {{ .Values.publicApi.ingress.pathType }}
            backend: {{- include "common.ingress.backend" (dict "serviceName" (include "basalt.public-api.fullname" .) "servicePort" "http" "context" $)  | nindent 14 }}
    {{- end }}
    {{- range .Values.publicApi.ingress.extraHosts }}
    - host: {{ .name | quote }}
      http:
        paths:
          - path: {{ default "/" .path }}
            pathType: {{ default "ImplementationSpecific" .pathType }}
            backend: {{- include "common.ingress.backend" (dict "serviceName" (include "basalt.public-api.fullname" $) "servicePort" "http" "context" $) | nindent 14 }}
    {{- end }}
    {{- if .Values.publicApi.ingress.extraRules }}
    {{- include "common.tplvalues.render" (dict "value" .Values.publicApi.ingress.extraRules "context" $) | nindent 4 }}
    {{- end }}
  {{- if or (and .Values.publicApi.ingress.tls (or (include "common.ingress.certManagerRequest" ( dict "annotations" .Values.publicApi.ingress.annotations )) .Values.publicApi.ingress.selfSigned)) .Values.publicApi.ingress.extraTls }}
  tls:
    {{- if and .Values.publicApi.ingress.tls (or (include "common.ingress.certManagerRequest" ( dict "annotations" .Values.publicApi.ingress.annotations )) .Values.publicApi.ingress.selfSigned) }}
    - hosts:
        - {{ .Values.publicApi.ingress.hostname | quote }}
      secretName: {{ printf "%s-tls" .Values.publicApi.ingress.hostname }}
    {{- end }}
    {{- if .Values.publicApi.ingress.extraTls }}
    {{- include "common.tplvalues.render" (dict "value" .Values.publicApi.ingress.extraTls "context" $) | nindent 4 }}
    {{- end }}
  {{- end }}
{{- end }}
