{{- if and .Values.scriptEvaluator.enabled .Values.scriptEvaluator.autoscaling.hpa.enabled }}
apiVersion: {{ include "common.capabilities.hpa.apiVersion" ( dict "context" $ ) }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "basalt.script-evaluator.fullname" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: script-evaluator
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: {{ include "common.capabilities.deployment.apiVersion" . }}
    kind: Deployment
    name: {{ include "basalt.script-evaluator.fullname" . }}
  minReplicas: {{ .Values.scriptEvaluator.autoscaling.hpa.minReplicas }}
  maxReplicas: {{ .Values.scriptEvaluator.autoscaling.hpa.maxReplicas }}
  metrics:
    {{- if .Values.scriptEvaluator.autoscaling.hpa.targetMemory }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.scriptEvaluator.autoscaling.hpa.targetMemory }}
    {{- end }}
    {{- if .Values.scriptEvaluator.autoscaling.hpa.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.scriptEvaluator.autoscaling.hpa.targetCPU }}
    {{- end }}
{{- end }}
